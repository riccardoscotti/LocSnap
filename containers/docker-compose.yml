version: '3'
services:
  backend:
    image: christianp01/cas_backend:latest
    network_mode: host
    command: npm start
    container_name: backend

  database:
    image: christianp01/cas_db_login:latest
    environment:
      - PGDATA=/var/lib/pgsql/data
      - POSTGRES_PASSWORD=contextawarerc
    network_mode: host
    container_name: db_login
